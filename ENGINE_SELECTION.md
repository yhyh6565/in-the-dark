# 🎮 개발 엔진 선택 가이드
## Engine Selection Guide - 검은 그늘 속에서

> **대상**: 1인 개발 초보자
> **게임 장르**: 8비트 픽셀아트 탑다운 호러 어드벤처
> **평가 기준**: 초보자 진입장벽 + 장기적 확장성

---

## 📋 비교 대상 엔진

1. **Godot 4** (추천 ⭐⭐⭐⭐⭐)
2. Unity
3. RPG Maker MV/MZ

---

## 1. Godot 4 ⭐⭐⭐⭐⭐ (최종 추천)

### ✅ 장점

#### 1.1 초보자 친화성
- **무료 & 오픈소스**: 라이선스 비용 없음, 수익 분배 없음
- **경량**: 설치 파일 100MB 미만, 낮은 사양 PC에서도 작동
- **직관적 UI**: 노드 시스템으로 게임 오브젝트 구조 이해 쉬움
- **내장 에디터**: 코드 에디터, 애니메이션 에디터, 타일맵 에디터 모두 포함
- **GDScript**: Python과 유사한 쉬운 스크립트 언어 (C#도 지원)

#### 1.2 이 게임에 최적화된 기능
- **2D 특화 엔진**: 탑다운 2D 게임에 완벽
- **내장 타일맵 시스템**: 학교 맵 제작이 매우 간편
- **Area2D**: 시선 시스템, 점멸 범위, 어그로 범위 구현 용이
- **Light2D**: 손전등 구현이 기본 기능으로 제공
- **AnimatedSprite2D**: 8비트 픽셀 애니메이션 쉬움
- **Fog of War**: CanvasModulate + Light2D 조합으로 구현 가능

#### 1.3 장기적 확장성
- **멀티플랫폼 빌드**: PC, Mac, Linux, 모바일, 웹 모두 지원
- **활발한 커뮤니티**: 한국어 커뮤니티 활성화, 튜토리얼 풍부
- **지속적 업데이트**: Godot 4.x 현재진행형 발전 중
- **확장 가능**: 나중에 3D, VR까지 가능 (필요 시)

### ❌ 단점
- **에셋스토어 규모**: Unity보다 작음 (하지만 픽셀아트는 직접 제작 권장)
- **레퍼런스 부족**: Unity/Unreal보다 한국어 자료 적음
- **최적화**: 대규모 프로젝트에서는 Unity보다 성능 낮을 수 있음 (이 게임은 해당 없음)

### 🎯 이 게임에서 사용할 핵심 기능

**시선 시스템**:
```gdscript
# RayCast2D로 시선 구현
var raycast = RayCast2D.new()
raycast.target_position = mouse_position - global_position
if raycast.is_colliding() and raycast.get_collider().is_in_group("student"):
    # 학생과 시선 마주침
```

**점멸 시스템**:
```gdscript
# Timer 노드로 점멸 주기 관리
var flicker_timer = Timer.new()
flicker_timer.wait_time = 300.0  # 5분
flicker_timer.timeout.connect(_on_flicker)
```

**손전등**:
```gdscript
# PointLight2D로 손전등 구현
var flashlight = PointLight2D.new()
flashlight.energy = 1.0
flashlight.texture = preload("res://textures/light_cone.png")
```

### 📚 추천 학습 경로

**1주차: 기초**
- 공식 튜토리얼: "Your First 2D Game" (https://docs.godotengine.org/en/stable/getting_started/first_2d_game/index.html)
- 유튜브: "GDQuest - Godot 4 기초" (한글 자막)

**2주차: 탑다운 게임**
- "HeartBeast - Top-Down RPG Tutorial" (Godot 3이지만 개념 동일)
- 타일맵 시스템 익히기

**3주차: 이 게임 특화 기능**
- 시선 시스템 (RayCast2D)
- 라이팅 시스템 (Light2D)
- AI 패트롤 (NavigationAgent2D)

**4주차: 프로토타입 제작**
- 2층 1개 교실 구현
- 학생 1명 AI 구현
- 점멸 시스템 구현

---

## 2. Unity ⭐⭐⭐☆☆

### ✅ 장점
- **방대한 에셋스토어**: 2D 픽셀아트 에셋 많음
- **레퍼런스 풍부**: 한국어 튜토리얼 가장 많음
- **취업 시장**: 게임 업계 표준 엔진
- **강력한 툴**: 프로파일러, 디버거 등 고급 기능

### ❌ 단점
- **무거움**: 설치 15GB 이상, 높은 사양 필요
- **복잡함**: 초보자에게 진입장벽 높음
- **2D 약점**: 2D는 부가 기능, 3D가 메인
- **라이선스**: 수익 $200,000 초과 시 유료 (Plus/Pro)
- **런타임 수수료**: Unity 6부터 설치 수 기반 수수료 논란

### 🤔 이 게임에 적합한가?
- **Yes**: 나중에 게임 업계 취업 목표라면
- **No**: 1인 개발 초보자, 2D 픽셀아트 게임에는 과한 선택

---

## 3. RPG Maker MV/MZ ⭐⭐☆☆☆

### ✅ 장점
- **코드 없이 제작**: 이벤트 에디터로 게임 로직 구현
- **빠른 프로토타입**: 맵 에디터, 캐릭터 생성기 내장
- **RPG 특화**: 인벤토리, 대화, 퀘스트 시스템 기본 제공

### ❌ 단점
- **제한적 커스터마이징**: 탑다운 2D RPG 외 장르 구현 어려움
- **시선 시스템 구현 불가능**: 이 게임의 핵심 메커니즘 구현 매우 어려움
- **점멸 시스템 구현 어려움**: 타이머 기반 순간이동 복잡
- **유료**: $79.99 (약 10만원)
- **확장성 제로**: 이 게임 외 다른 장르 개발 불가

### 🚫 이 게임에 적합하지 않음
RPG Maker는 **턴제 RPG** 전용 툴입니다. 이 게임은:
- 실시간 추격 시스템 (RPG Maker에서 매우 어려움)
- 시선 메커니즘 (플러그인 없이 불가능)
- 점멸 순간이동 (이벤트로 구현 시 버그 많음)

**결론**: RPG Maker는 부적합

---

## 🏆 최종 추천: Godot 4

### 선택 이유

1. **완전 무료**: 라이선스 걱정 없음, 수익 100% 본인 소유
2. **초보자 최적**: 경량, 직관적, 학습 곡선 낮음
3. **2D 게임 최강**: 타일맵, 라이팅, 애니메이션 모두 내장
4. **이 게임 메커니즘 완벽 지원**:
   - 시선 시스템 (RayCast2D)
   - 점멸 시스템 (Timer + 어둠 효과)
   - 손전등 (Light2D)
   - Fog of War (CanvasModulate)
5. **장기적 확장**: 멀티플랫폼, 3D 확장 가능

### Unity와 비교
| 항목 | Godot 4 | Unity |
|:---:|:---:|:---:|
| **설치 크기** | 100MB | 15GB+ |
| **학습 기간** | 2-4주 | 2-3개월 |
| **2D 특화** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐☆☆ |
| **라이선스** | 무료 | 조건부 유료 |
| **이 게임 적합도** | 100% | 70% |

---

## 🚀 Godot 4 시작 가이드

### Step 1: 설치 (5분)
1. https://godotengine.org/download 접속
2. "Godot Engine - Standard version" 다운로드
3. 압축 해제 후 실행 (설치 과정 없음)

### Step 2: 첫 프로젝트 생성 (10분)
1. "New Project" 클릭
2. Project Name: "InTheDark"
3. Renderer: "Forward+" 선택 (2D 게임이지만 라이팅 사용)
4. "Create & Edit" 클릭

### Step 3: 필수 학습 (1주)
**공식 문서**:
- https://docs.godotengine.org/en/stable/getting_started/introduction/index.html

**유튜브 한글 튜토리얼**:
- "GDQuest Godot 4 튜토리얼 시리즈"
- "HeartBeast Top-Down RPG"

**커뮤니티**:
- 디스코드: Godot Engine 한국 커뮤니티
- 레딧: r/godot

### Step 4: 프로토타입 제작 (2-4주)
**Week 1**: 플레이어 이동, 타일맵, 충돌
**Week 2**: 학생 AI, 시선 시스템
**Week 3**: 점멸, 손전등, 어둠
**Week 4**: UI, 인벤토리, 명찰

---

## 📦 Godot 4 프로젝트 구조 권장안

```
InTheDark/
├── assets/
│   ├── sprites/
│   │   ├── player/
│   │   ├── students/
│   │   ├── npcs/
│   │   └── objects/
│   ├── tilesets/
│   │   ├── floor_tiles.png
│   │   ├── wall_tiles.png
│   │   └── object_tiles.png
│   ├── audio/
│   │   ├── bgm/
│   │   └── sfx/
│   └── fonts/
├── scenes/
│   ├── main.tscn (메인 씬)
│   ├── floors/
│   │   ├── floor_1.tscn
│   │   ├── floor_2.tscn
│   │   └── ...
│   ├── characters/
│   │   ├── player.tscn
│   │   └── student.tscn
│   └── ui/
│       ├── hud.tscn
│       └── inventory.tscn
└── scripts/
    ├── player.gd
    ├── student_ai.gd
    ├── flicker_system.gd
    └── game_manager.gd
```

---

## 🔧 Godot에서 구현할 핵심 시스템 예시

### 1. 점멸 시스템
```gdscript
# FlickerSystem.gd
extends Node

@export var flicker_interval := 300.0  # 5분
@export var warning_time := 5.0  # 5초 예고

var timer: Timer
var warning_timer: Timer

func _ready():
    timer = Timer.new()
    timer.wait_time = flicker_interval
    timer.timeout.connect(_on_flicker_warning)
    add_child(timer)
    timer.start()

    warning_timer = Timer.new()
    warning_timer.one_shot = true
    warning_timer.wait_time = warning_time
    warning_timer.timeout.connect(_on_flicker_execute)
    add_child(warning_timer)

func _on_flicker_warning():
    # 5초 예고
    EventBus.emit_signal("flicker_warning")
    warning_timer.start()

func _on_flicker_execute():
    # 1초 암전 + 학생 순간이동
    EventBus.emit_signal("flicker_execute")
    await get_tree().create_timer(1.0).timeout
    # 학생들 재배치
    get_tree().call_group("students", "teleport_random")
    timer.start()  # 타이머 리셋
```

### 2. 시선 시스템
```gdscript
# Player.gd
extends CharacterBody2D

var gaze_line: RayCast2D

func _ready():
    gaze_line = RayCast2D.new()
    add_child(gaze_line)

func _process(delta):
    # 마우스 방향으로 시선
    var mouse_pos = get_global_mouse_position()
    gaze_line.target_position = to_local(mouse_pos)

    if gaze_line.is_colliding():
        var collider = gaze_line.get_collider()
        if collider.is_in_group("student"):
            collider.on_gazed()  # 학생에게 추격 시작 신호
```

### 3. 손전등
```gdscript
# Flashlight.gd
extends PointLight2D

@export var battery_max := 600.0  # 10분
var battery_current := battery_max

func _process(delta):
    if Input.is_action_pressed("flashlight"):
        enabled = true
        battery_current -= delta
        if battery_current <= 0:
            enabled = false
    else:
        enabled = false

    # 배터리 UI 업데이트
    EventBus.emit_signal("battery_changed", battery_current / battery_max)
```

---

## ✅ 결론

**Godot 4를 강력 추천합니다.**

1인 개발 초보자가 8비트 픽셀아트 탑다운 호러 게임을 만들기에 **가장 적합한 엔진**입니다.

**학습 투자 대비 효율**: ⭐⭐⭐⭐⭐
**이 게임 구현 난이도**: ⭐⭐☆☆☆ (쉬움)
**장기적 확장성**: ⭐⭐⭐⭐⭐

Unity는 3D 게임이나 팀 프로젝트, 취업 포트폴리오용이라면 고려할 수 있지만, 이 게임에는 과한 선택입니다.

---

**문서 버전**: 1.0
**최종 수정일**: 2026-02-17
**다음 문서**: ASSET_LIST.md (에셋 리스트)
